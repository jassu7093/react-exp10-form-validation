# react-exp10-form-validation
To create a **validated registration form** in React that checks for: - A valid email format   - A password length greater than 6 characters   - Displays user-friendly error messages dynamically
